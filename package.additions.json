{
  "scripts": {
    "__ensure-babel-deps": "node -e \"try{require('@babel/parser')&&require('@babel/traverse')&&require('@babel/generator')&&process.exit(0)}catch(e){process.exit(0)}\"",
    "prebuild": "node scripts/prebuild-tdz.cjs"
  },
  "devDependencies": {
    "@babel/parser": "^7.25.0",
    "@babel/traverse": "^7.25.0",
    "@babel/generator": "^7.25.0"
  }
}